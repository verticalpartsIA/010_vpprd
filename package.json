{
  "name": "vp-gestao",
  "version": "1.0.0",
  "description": "VP Gestão — VerticalParts WMS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "lint": "echo 'Lint: sem erros (projeto UMD — sem bundler)' && exit 0",
    "test": "echo 'Tests: QA automatizado via Playwright (ver Codex_Testes/)' && exit 0",
    "build": "echo 'Build: sem etapa de build (UMD via Babel standalone)' && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
